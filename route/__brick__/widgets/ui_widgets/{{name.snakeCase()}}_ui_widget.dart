import 'package:flutter/material.dart';

class {{name.pascalCase()}}UiWidget extends StatelessWidget {
  const {{name.pascalCase()}}UiWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
